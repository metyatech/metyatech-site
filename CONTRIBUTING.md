# Contributing

## Development flow

1. Install dependencies with `npm install`.
2. Regenerate `AGENTS.md` with `npx compose-agentsmd --compose` when rules change.
3. Run `npm run verify` before commit.

## Scope

Keep changes focused on the public `metyatech.com` site and its product and policy pages.
